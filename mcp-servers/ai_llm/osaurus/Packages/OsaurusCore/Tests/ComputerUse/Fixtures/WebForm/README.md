# Computer Use Web Form Fixture

This fixture is a static local form used by the Computer Use proof lane. It is
not served from the network and it has no external scripts, images, fonts, or
stylesheets.

The Core evidence test mirrors the controls in `request.html` into a
deterministic accessibility scene so the proof can exercise perceive -> gate ->
act -> verify without real browser automation or screenshot capture.
